# 🌸 数字花园 — 零基础使用指南

## 怎么打开网站

### 每次启动步骤

1. 按键盘 `Win + R`，输入 `powershell`，回车
2. 复制下面这行命令，在窗口里右键粘贴，回车：

```
cd D:\web && npx next dev
```

3. 等十几秒，看到屏幕上出现 `✓ Ready` 就说明启动好了
4. 打开浏览器，地址栏输入 **http://localhost:3000** 就能看到你的数字花园

### 怎么关闭

在命令行窗口里按 `Ctrl + C`，然后关掉窗口就行。

---

## 用手机查看

1. 确保手机和电脑连接的是同一个 WiFi
2. 启动后在命令行里找这行：

```
- Network:       http://10.183.xxx.xxx:3000
```

3. 在手机浏览器输入上面那个地址（是你电脑的 IP，每次可能不同）

---

## 项目文件夹说明

```
D:\web\
  ├── README.md              ← 你现在看的这个文件
  ├── supabase-schema.sql    ← 数据库脚本（目前用不到）
  ├── .env.local             ← 配置文件（里面有 Supabase 连接信息）
  └── src\                   ← 所有源代码
      ├── app\               ← 各个页面
      │   ├── page.tsx               首页
      │   ├── (main)\resources\     资源库
      │   ├── (main)\categories\    分类
      │   ├── (main)\tags\          标签
      │   └── (auth)\               登录/注册
      ├── components\         ← 界面组件
      │   ├── layout\               导航栏、底部栏
      │   ├── resources\            资源卡片、表单
      │   ├── shared\              搜索框、分页等
      │   └── ui\                   通用UI组件
      └── lib\               ← 数据和工具
          ├── supabase\            数据库连接
          ├── db\                  数据增删改查
          └── types\              类型定义
```

---

## 当前功能状态

| 功能 | 状态 | 说明 |
|------|------|------|
| 浏览首页 | ✅ 正常 | 统计卡片、分类入口、实时数据 |
| 笔记 | ✅ 正常 | 瀑布流布局、搜索、标签 |
| 资源库 | ✅ 正常 | CRUD、分类筛选、标签、搜索 |
| 合集 | ✅ 正常 | 创建合集、归类资源 |
| 时间线 | ✅ 正常 | 按日期分组、隐藏笔记 |
| 分类浏览 | ✅ 正常 | 按分类查看内容 |
| 标签管理 | ✅ 正常 | 创建/编辑/删除标签 |
| 音乐播放 | ✅ 正常 | 上传/播放/管理本地音频 |
| 文件管理 | ✅ 正常 | 上传/下载/管理文件 |
| 搜索资源 | ✅ 正常 | 按 Ctrl+K 快速搜索 |
| 暗色/亮色模式 | ✅ 正常 | 点右上角太阳/月亮图标 |
| 手机浏览 | ✅ 正常 | 响应式适配 |
| 密码保护 | ✅ 正常 | 中间件密码门 |
| 添加资源 | ✅ 正常 | 支持 Supabase 或本地存储 |
| 登录注册 | ⏳ 待配置 | 需要先配置 Supabase |

---

## 配置数据库（让添加功能能用）

1. 打开浏览器，访问 https://supabase.com
2. 点 "Start your project" 用 GitHub 账号注册（免费）
3. 创建一个新项目，名字填 `digital-garden`
4. 创建好后，左侧菜单找到 "SQL Editor"
5. 把 `D:\web\supabase-schema.sql` 文件内容复制粘贴进去，点 Run
6. 左侧菜单找到 "Project Settings" → "API"
7. 复制 `Project URL` 和 `anon public key`
8. 打开 `D:\web\.env.local`，替换对应的值：
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://你的项目ID.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=你的anon key
   ```
9. 重新启动 `npx next dev`，所有功能就都能用了

---

## 常见问题

**Q: 打开网页显示空白或报错？**
A: 确保命令行窗口没有关闭，看到 `✓ Ready` 才算启动成功。

**Q: 提示端口被占用？**
A: 把命令改成 `npx next dev -p 3001`，然后访问 `http://localhost:3001`。

**Q: 怎么更新代码？**
A: 我会帮你修改，你只需要重启服务（按 Ctrl+C 停掉，再重新运行 `npx next dev`）。

---

## VPS Nginx 静态资源缓存配置建议（阶段 2.4）

> ⚠️ 本节仅为配置建议文档，不自动应用。需手动 SSH 到 VPS 修改 Nginx 配置。
> **操作前必须备份 `/etc/nginx/nginx.conf`，并确认不影响 Xray :8443 代理。**

### 目标
对 VPS 存储上的静态文件（`/storage/*`）和 Next.js 静态资源添加长缓存 `Cache-Control` 头，减少重复下载、节省带宽。

### 建议配置（添加到 Nginx 的 `server` 或 `location` 块）

```nginx
# VPS 存储静态文件 — 音乐/图片/文档等。文件名含 UUID，可长缓存。
# 加在 storage.minitu.online 的 server 块内。
location /storage/ {
    # 允许 GET/PUT/HEAD，按需添加 DELETE（见下方 DELETE 405 修复说明）
    dav_methods PUT;
    limit_except GET HEAD PUT {
        deny all;
    }

    # 长缓存：文件路径含 UUID，内容不变即可长缓存
    add_header Cache-Control "public, max-age=2592000, stale-while-revalidate=86400" always;

    # 启用 range 支持（音乐播放需要）
    add_header Accept-Ranges bytes;
}

# Next.js 构建产物（如果 Next.js 也部署在 VPS 上）
location /_next/static/ {
    add_header Cache-Control "public, max-age=31536000, immutable" always;
}
```

### ⚠️ DELETE 405 修复（阶段 2 遗留）

当前 VPS 存储 `DELETE` 返回 405（Nginx `dav_methods` 未启用 DELETE）。修复方式：

```nginx
location /storage/ {
    dav_methods PUT DELETE;   # 添加 DELETE
    # ...其余配置不变
}
```

修改后 `nginx -t` 测试配置，确认无误后 `systemctl reload nginx`。
**操作前确认**：此改动仅影响 `/storage/` location 块，不涉及 :8443 端口的 Xray server 块，理论上不影响代理。但仍建议先备份配置文件。

### 验证

```bash
# 缓存头验证
curl -I https://storage.minitu.online/storage/music/test.mp3
# 应看到: Cache-Control: public, max-age=2592000, stale-while-revalidate=86400

# DELETE 修复验证
curl -X DELETE -I https://storage.minitu.online/storage/test-delete.txt
# 应看到 200 或 204（不再是 405）
```
